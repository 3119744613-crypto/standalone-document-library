# 独立源码版验收

日期：2026-09-23。环境：macOS arm64、Node.js 24.19.0。

## 已通过

- 独立目录运行 `node scripts/test.mjs`：**43/43，0 失败、0 跳过**，包括 25 项合成 HTTP 契约及 18 项 UI 状态测试。实际从其他工作目录调用脚本，测试能定位本仓库文件。
- `node scripts/build.mjs`：退出 0；页面 JavaScript 语法检查、3 个静态文件及 SHA256 清单生成成功。这不是 Yuxi 后端构建。
- `sh -n start.sh`：退出 0。将源码复制到含空格的新目录，从无关目录调用 `start.sh`：主页 HTTP 200，状态接口明确 `backendConfigured=false`、`not_configured`；之后正常停止测试进程。
- 限定范围只读检查：运行代码仅依赖 Node 内置模块和本仓库相对路径；没有其他业务系统、Agent 或任意上游代理接口。没有复制其他仓库的 Git 历史、业务代码、数据库、凭据或运行产物。

## 失败、修复与未满足条件

- `doctor` 实际退出 2：没有配置上游 Yuxi，不是完整部署通过。
- 提取后文档中的旧目录和外部文档路径已改为本仓库路径；doctor 的 Node 要求从 22 统一为 24。
- Windows 启动脚本经静态复核移除了可能受旧 PowerShell 引号传递影响的写法；没有 Windows 实机结果。
- 整理文档的首次编辑工具请求因同一路径的重复操作被拒绝，未修改文件；调整编辑方式后成功。这不是产品测试失败。

## 未验证（NOT_RUN）

- 真实 Yuxi 后端部署、实际模型/embedding 配置、上传到索引的真实生命周期。
- 真实两账号权限、撤权、下载与物理删除、检索质量、容量和长期运行。
- Windows/Linux 实机、其他浏览器和浏览器完整上传生命周期。轻量 UI 状态测试不能替代浏览器全流程。
- 远程发布是否完成以实际 GitHub 仓库和发布回执为准；本文不作为已上传证明。

## 回滚与数据

按 Ctrl-C 停止入口。保留自己的独立环境配置后，可切换到本仓库此前确认过的版本；本版本是独立初始交付。不要将源码回滚理解为上游文档写入或删除的回滚。原第三方许可通知应随源码保留。
