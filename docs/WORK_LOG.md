# 独立通用资料库工作日志

## 2026-09-23 — 提取和移机检查

- 建立全新工作目录，仅按明确清单提取已有独立资料模块的 21 个文件，没有复制原仓库历史、业务代码、数据库、运行缓存或账户配置。
- 重写独立 README、接口说明和第三方通知；增加 macOS/Linux 和 Windows 启动入口，使用按脚本自身位置定位的跨目录测试入口。没有给原创适配器擅自选择开源许可证。
- 初次编辑工具请求将同一路径同时删除和新增，被工具拒绝；文件未因此改变。拆分编辑后完成。
- 清理旧目录文档引用；统一 Node 24 最低版本。静态审查发现 PowerShell 嵌套引号兼容性风险，改为不包含嵌套字符串的版本检查。Windows 实机未运行。
- 从无关工作目录执行合成测试，43/43 通过；独立静态构建通过；未配置上游时 doctor 退出 2，保留为部署条件未满足。
- 将源码复制到含空格的新目录，运行 start.sh；主页及状态接口 HTTP 200，状态准确为 not_configured；验收进程随后停止。
- 真实 Yuxi 后端、模型、权限和存储未验收。该版本没有其他 Agent 或业务工作流的连接接口。GitHub 发布状态由实际远程回执单独记录，不把本地提交视为已发布。

## 2026-09-23 — 原生本地资料库改造开始

- 用户确认 Yuxi 仅作为知识库设计参考，不部署或连接其后端。改造限定在独立仓库，不修改原 Agent、原数据库或原业务链路。
- 起点 `dc9d23d50ba3c548f0b09b6f3c9f3d7a65e98f50`，分支 `main`，工作树干净。旧版本和交付包保留，未覆盖未提交修改。未发现本独立仓库适用的 AGENTS.md、CONTRIBUTING.md。
- 环境 macOS arm64、Node 24.19.0；内置 SQLite 3.53.3 可用。旧版基线测试 43/43 通过（退出 0）、静态构建通过（退出 0）；doctor 退出 2：未配置旧版要求的 Yuxi，属原有部署依赖，不是本轮新增缺陷。
- 选择单机单账户、原生 SQLite、UTF-8 Markdown/TXT、关键词检索；账号初始化、资料库、原文与索引由本模块自行管理，不调用外部模型。多用户授权、向量语义检索、PDF 仍不属于本轮交付。
- 编辑工具首次替换 doctor 时把同一路径写成删除加新增，工具拒绝该请求，未改变该文件；改为单次写入完成。保留该操作失败记录。
- 用户追加要求：在 GitHub 提供与 wangc219/deep-research 的版本差异说明。已按固定上游提交编写 CHANGES_FROM_DEEP_RESEARCH.md，并在 README 链接，明确独立仓库关系和未迁入范围。
- 前端移除旧服务登录、API Key 和模型配置，新增首次设置及真实会话注销。首轮旧 UI 测试 17 项失败：测试桩未读取 HTML 初始 disabled；修复桩后旧 18 项通过，新增 8 项也通过，共 26/26。浏览器验收此时仍未执行。
- 后端新增本地 SQLite、加盐密码、散列会话、文档与索引事务；删除旧上游运行模块。解析保留编码失败状态；目录独占，拒绝未知较新数据库版本，异常停止保留明确恢复提示。
- 首次新 HTTP 测试 17 项因 EPERM 无法监听本机端口；获准后 17/17 通过。新的测试文件替换曾遇到编辑工具同路径重复操作拒绝，调整方式后成功。
- 真实契约复核修复了处理按钮过度开放；新增初始化取消恢复测试。浏览器发现初始化后顶部仍显示“请创建账号”，已修复并补充现有断言。最终 UI 28/28。
- 浏览器实际完成首次设置、创建并打开资料库；文件选择调用出现长时间等待且未确认上传，不写为完整浏览器验收通过。
- 最终 node scripts/test.mjs 退出 0：51/51，含 HTTP 17、存储 6、UI 28；构建和环境检查均退出 0，git diff --check 退出 0。没有运行 Windows/Linux、容量或完整浏览器流程。
- 回滚保留初始提交与旧源码包；先停止服务并备份独立数据目录。代码回退不能恢复被删除资料；旧 0.1 不读取新本地数据库。
- 独立最终复核未发现运行依赖或发布清单中的用户资料泄漏；发布仅含 32 个源码、测试与说明文件。首次本地提交自动采用机器身份，远程发布前改为与初始提交一致的 Codex 身份，不发布本机邮件地址。
