# Reference only: the program does not auto-load .env files.
# Optional local settings; no external backend or model service is required.
LIBRARY_PORT=4184
# Default is .runtime beside server.mjs. Use an absolute path for a custom location.
# LIBRARY_DATA_DIR=/absolute/path/to/private-library-data
