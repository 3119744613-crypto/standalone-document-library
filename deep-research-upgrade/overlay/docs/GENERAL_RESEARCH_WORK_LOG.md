# 通用研究工作台升级日志

## 2026-09-23 开始

- 用户确认以 wangc219/deep-research 现有代码升级通用部分；基线49edb184642009d1f1994d41b63853d20315233d，工作树干净。创建feature/general-research-workbench分支，原提交保留。
- 已读取实际 CONTRIBUTING.md、前次验收与工作日志。修改路径没有适用AGENTS.md。
- 已确认：真实多Agent、先审计划、PDF/DOCX/MD/TXT、GitHub源码交付、个人本机运行、自己的模型密钥、必要片段可发送指定模型；原领域业务不连接新模块。
- 复用原生资料库2d79001到apps/general-research，不复制其.git，不覆盖旧库或原数据库。
- 基线make check退出0。两个前端基线命令首次因日志脚本尚未创建退出2，重新按依赖顺序执行；这是编排错误，不计作产品测试。

- 独立环境安装首次完成Python解析依赖，npm阶段因沙箱不可写全局 ~/.npm 缓存失败（EPERM），未修改缓存权限；构建脚本改用本模块 .build/npm-cache 后重跑。
- 独立审查复现慢请求在退出后仍能写入资料，正在加正文读取后的统一认证复查；保留复现及修复测试。

- 独立安装重跑通过：Python依赖在模块.venv，npm在.build/web，产物dist-general；原环境不变。首次真实浏览器验收发现登录页不能发API请求，原因是默认fetch作为对象方法调用失去浏览器要求的全局接收者；包装绑定后新增测试通过，重建后页面连接、登录和创建草稿已实际通过。
- 独立审查先复现慢正文退出后写入，再修复所有相关请求重验认证。另新增回归实测30/33通过、3失败：搜索词边界不一致、SSE终态遗漏200条后的事件、公网地址范围误拒绝。修复后专项33/33通过；补充真实本机HTTP断开、Last-Event-ID重连、取消及迟到响应保护测试，HTTP共6/6通过。
- 前端专项首次合跑93项中92通过，1项监听127.0.0.1遭沙箱EPERM；限定本机监听的授权执行重跑93/93通过。不是产品功能失败。

- 最终模块98/98通过、前端164/164通过、原make check及doctor通过。浏览器实际通过账号登录、草稿、计划编辑确认、五角色报告、来源预览、关键词检索、等待审核时取消、任务切换、退出清空；请求路径记录未出现原业务API。所有模型/搜索均为合成，不算真实服务验收。
- 浏览器下载事件监听10秒超时，随后发现真实Markdown下载文件（935字节，SHA256 17fefa5e32e4fc0f6f6449efd73f7ded8489a9d8631145f342ab46fbb8e7f9b1），核对合成标签及S1/S2通过。四格式浏览器文件选择器、浏览器强制断网未执行。
- 侧栏完成状态滞后已修复；重载新构建后重新登录、重试规划，侧栏与详情的等待确认/取消状态自动一致，无需人工刷新。

## 升级包应用核对

- 在官方 9b67368c 基线的独立稀疏克隆中，预检查与应用均通过，55 个新增文件摘要一致。稀疏克隆只包含升级所需源码，未启动或验收原领域业务。
- 人为加入已跟踪文件未提交修改和同名未跟踪文件冲突，应用器均按预期拒绝，原内容不变；这两次退出 1 是保护用例通过。
- 分发采用新增 overlay 加四个文件的导航补丁，未打包原配置、数据库、依赖目录和历史领域代码。
